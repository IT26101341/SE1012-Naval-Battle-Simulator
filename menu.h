#ifndef MENU_H
#define MENU_H

#include "types.h"

/* Functions used to show instructions and control the two main menus. */
void show_instructions(void);
void start_menu(Game *game);
void main_menu(Game *game);

#endif
