#include <stdio.h>
/* Day 1 verifies the compiler and project layout. */
int main(void)
{
    puts("Advanced Naval Battle Simulator");
    puts("Project structure and C11 build system are ready.");
    return 0;
}
